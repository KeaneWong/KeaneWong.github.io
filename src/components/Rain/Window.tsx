// import {planeGeometry} from "@react-three/drei";

export const Window = () =>{
    return(
            <planeGeometry
                args={[
                    2,2,1,1
                ]}
                // width={2}
                // depth={2}
                // segmentsWidth={1}
                // segmentsDepth={1}
            />
    )
}